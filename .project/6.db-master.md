# DESAIN DATABASE MASTER

📌 CATATAN PENTING :
- Semua createdAt / updatedAt → text NOT NULL
- Semua enum → text + Zod, bukan enum DB. WAJIB import dari `shared/utils/constant.ts`
- Semua FK → integer

## 1 **Vendor**
| Kolom     |Tipe    |Constraint        |Keterangan         |
|---------  |--------|------------------|-------------------|
|id         |integer |PK                |ID vendor          |
|name       |text    |NOT NULL, UNIQUE  |Nama vendor        |
|isActive   |integer |NOT NULL          |Boolean            |
|createdAt  |integer |NOT NULL          |Waktu dibuat       |
|updatedAt  |integer |NOT NULL          |Waktu ada update   |



INDEX :
- UNIQUE (name)

📌 CATATAN PENTING : data awal vendor : `MOKA`, `MTC`, `SDP`

KEPUTUSAN FINAL:
✅ SOFT DELETE untuk Vendor
Implementasi:

Vendor tidak akan benar-benar dihapus dari database
Gunakan flag isActive: boolean untuk non-aktifkan
Semua relasi (ProductModel, NotificationRef, Claim, dll) TIDAK PERLU onDelete: 'restrict'
Vendor yang isActive = false tidak muncul di dropdown, tapi data historis tetap utuh

--------------------------------------------------------------------------

## 2 **ProductModel**
|Kolom     |Tipe    |Constraint        |Keterangan     |
|--------- |--------|------------------|---------------|
|id        |integer |PK                |ID model       |
|name      |text    |NOT NULL, UNIQUE  |Nama Model     |
|inch      |integer |NOT NULL          |Ukuran inch    |
|vendorId  |integer |FK -> vendor.id   |Kode vendor    |
|isActive  |integer |NOT NULL          |Boolean        |
|createdAt |integer |NOT NULL          |Waktu dibuat   |
|updatedAt |integer |NOT NULL          |Waktu update   |

INDEX :
- UNIQUE (name)
- INDEX (vendorId)

📌 CATATAN PENTING : data produk model tetap ada walaupun vendor di nonaktifkan/didelete
--------------------------------------------------------------------------

## 3 **NotificationRef**
| Kolom             |Tipe    |Constraint        |Keterangan            |
|-------------------|--------|------------------|----------------------|
|id                 |integer |PK                |ID notificatin ref    |
|notificationCode   |text    |UNIQUE            |Kode notifikasi       |
|modelName          |text    |NOT NULL          |Nama model            |
|branch             |text    |NOT NULL          |Cabang CS             |
|vendorId           |integer |FK -> vendor.id  |Kode vendor          |
|status             |text    |NOT NULL          |NEW / USED / EXPIRED  |
|createdBy          |integer |FK -> user.id     |Nama QRCC             |

INDEX :
- UNIQUE (notificationCode)
- INDEX (vendorId)

📌 CATATAN PENTING : data notifikasi ref tetap ada walaupun vendor & product model di nonaktifkan/didelete

--------------------------------------------------------------------------

## 4 **VendorPhotoRule**
|Kolom       |Tipe    |Constraint        |Keterangan            |
|------------|--------|------------------|----------------------|
|id          |integer |PK                |ID VendorPhotoRule    |
|vendorId    |integer |FK -> vendor.id   |Kode vendor          |
|photoType   |text    |NOT NULL          |                      |
|isRequired  |integer |NOT NULL          |Boolean               |
|createdAt   |integer |NOT NULL          |Waktu dibuat          |
|updatedAt   |integer |NOT NULL          |Waktu ada update      |

INDEX :
- UNIQUE (vendorId, photoType)
- INDEX (vendorId)

📌 Validasi ENUM -> backend/Zod CLAIM / CLAIM_ZOOM / ODF / PANEL_SN / WO_PANEL / WO_PANEL_SN

| photoType   | MOKA | MTC | SDP | 
|-------------|------|-----|-----|
| CLAIM       | ✅   | ✅  | ✅  |
| CLAIM_ZOOM  | ✅   | ✅  | ✅  |
| ODF         | ✅   | ✅  | ✅  |
| PANEL_SN    | ✅   | ✅  | ✅  |
| WO_PANEL    | ✅   | ❌  | ❌  |
| WO_PANEL_SN | ✅   | ❌  | ❌  |

--------------------------------------------------------------------------

## 5 **VendorFieldRule**
|Kolom       |Tipe    |Constraint        |Keterangan            |
|------------|--------|------------------|----------------------|
|id          |integer |PK                |ID VendorFieldRule    |
|vendorId    |integer |FK --> vendor.id  |Kode vendor           |
|fieldName   |text    |NOT NULL          |odfNumber / version / week |
|isRequired  |integer |NOT NULL          |Boolean               |
|createdAt   |integer |NOT NULL          |Waktu dibuat          |
|updatedAt   |integer |NOT NULL          |Waktu ada update      |

INDEX :
- UNIQUE (vendorId, fieldName)
- INDEX (vendorId)

📌 Panduan VendorFieldRule:
| No  | Field Name  | MOKA  | MTC  | SDP |
| ----| ----------  | ----- | ---- | ----|
| 1   | odfNumber   | ✅    | ❌   | ❌  |
| 2   | version     | ✅    | ❌   | ❌  |
| 3   | week        | ✅    | ❌   | ❌  |

--------------------------------------------------------------------------

## 6 **DefectMaster**
|Kolom      |Tipe    |Constraint        |Keterangan            |
|-----------|--------|------------------|----------------------|
|id         |integer |PK                |ID DefectMaster       |
|name       |text    |NOT NULL, UNIQUE  |nama defect           |
|isActive   |integer |NOT NULL          |Boolean               |
|createdAt  |integer |NOT NULL          |Waktu dibuat          |
|updatedAt  |integer |NOT NULL          |Waktu ada update      |

INDEX :
- UNIQUE (name)