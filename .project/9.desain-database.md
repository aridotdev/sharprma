# DESAIN DATABASE

📌 CATATAN PENTING :
- Semua createdAt / updatedAt → text NOT NULL
- Semua enum → text + Zod, bukan enum DB. WAJIB import dari `types/`
- Semua FK → integer

## 1. Master
## 2. Transaksi & Audit
## 3. User
## 4. PRINSIP DESAIN PENTING
-   Auth ≠ Business User
-   Master vs Snapshot Transaction
-   Append-only audit log
-   Vendor rules data-driven
-   Tidak hardcode logic vendor
-   DB ringan, logic di app layer
-   Relational Database (SQLite)

### Separation of Concerns
|Layer      |Tanggung Jawab                     |Tidak Boleh                    |Folder                                 |
|API Route  |HTTP, Auth, Validasi input dasar   |Business logic, Query DB       |server/api/*                           |
|Service    |Business logic, Koordinasi         |Query DB langsung, HTTP stuff  |server/service/*.service.ts            |
|Repository |CRUD database                      |Business logic, Auth           |server/repositories/*.repository.ts    |

server/
  ├── api/*                     --> untuk API endpoint route  
  ├── services/                 --> Business Logic
  └── repositories/             --> DB Operations


### Timestamp Format
createdAt: integer({ mode: 'timestamp' })
    .notNull()
    .default(sql`(unixepoch())`)

